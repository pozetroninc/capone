# Setup requirements are now defined in pyproject.toml [build-system].requires
# This file might no longer be needed, depending on Makefile usage.
# Keeping it empty for now.
# tox is a test runner, not a setup requirement.
